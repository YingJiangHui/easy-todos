#!/usr/bin/env node
const {add} =require('./function/add')
const {remove} = require('./function/remove')
const clear = require('./function/clear')
const show = require('./function/show')

module.exports = {add, remove, clear, show}


